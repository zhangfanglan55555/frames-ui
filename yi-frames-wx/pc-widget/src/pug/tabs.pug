mixin tabs(num, ...items)
	.yi-tabs&attributes(attributes)
		ul.tab-nav.tabs-ul
			each item, index in items
				li(class=num==index?'active':'') #{item}
					//- a.link(href="#") #{item}
		//.tab-panel
		if block
			block